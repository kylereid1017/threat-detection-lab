# HANDOFF — Threat Detection Lab: swarm review, fixes, errata (2026-09-10)

> Companion to `handoff.md` (the agent-capability-composition programme, still current for its scope).
> Read both. This doc covers the swarm programme and everything done 2026-09-10.

## START HERE

**Current state:** The swarm review is complete and the code fixes are landed and verified (485/485 tests, 86% coverage, deterministic artifacts). The remaining work is the *numbers pipeline rebuild* — per-observation records, denominator semantics, bounded endurance re-run, cable regeneration — and the **commit/share decision**. Nothing is broken; do not "fix" the current `docs/swarm/results/boundary_map_*.json` — they are the correct, deterministic outputs.

**First action (needs Kyle's input):** decide the share strategy — everything since HEAD (Sep 4) is uncommitted (~130 dirty entries incl. `docs/reviews/`, the whole agent_graph programme, the trust-repair work). Recommend: commit a reviewed subset + push, so the review→repair history is the credibility. Until that's decided, **commit the 5 code modules touched this session at minimum** — there is no versioned rollback for them.

**Verification ledger — what is MEASURED vs what KYLE has LOOKED AT:**

- Measured (by agent, this session): 485/485 tests pass (~7.8s, re-run 3×); statement coverage 86% (re-measured, floor 85%); review reproductions 9/9; swarm CLI artifacts byte-deterministic across runs; regenerated `boundary_map_yara.json`/`campaign_report_yara.md` now byte-identical to committed HEAD (git diff empty); `--synthesize-trends` refuses empty basis (exit 1); per-target boundary histories reconcile exactly with real counters; IPv6 rejection live; walk-profile rotation live (6 offsets → 4 distinct interception nodes, MTTD 0/60/90s).
- **NOT SEEN by Kyle:** all of the above, plus the errata, the architecture.md rewrite, and the code diffs. **No human sign-off on any of this session's work.** The fixes are agent-verified only. Kyle's gate: "runs" and "sounds good" are separate — treat them as such.

**Theory of the project:** the lab measures "what can my tools actually see" — agent tool composition (lethal trifecta) and detection boundary mapping — with reproducible, LLM-free, sandboxed harnesses. Governing rule: **every published number must be reproducible from persisted state**; honesty about scope beats breadth of claims. This session existed to make the numbers layer survive that rule.

**Rollback:**
- Artifacts backup: `C:\Users\kyler\AppData\Local\Temp\tdl_results_backup_1789065361` (pre-session `docs/swarm/results/`), hash baselines `tdl_results_hash_before.txt` / `tdl_results_hash_after.txt` in the same Temp dir.
- **Code: NO versioned rollback.** The 5 edited modules exist only in the working tree. Revert by reversing the patches manually (diffs were shown in session) or by re-cloning HEAD and re-applying the pre-existing uncommitted work. This is why committing the code files is thread #1.

**User's working style:** prefers the agent to execute directly (not copy-paste instructions); values rigorous independent verification — real measurements over self-reports, honest scope checks over agreement; tight reports, no filler.

---

## What this is

`threat-detection-lab` (C:\Users\kyler\Projects\threat-detection-lab) — a detection-engineering + AI-agent-security portfolio lab. Two programmes:

1. **Agent capability composition** (`tools/agent_graph/`) — "lethal trifecta" (Private Data + Untrusted Ingress + Exfiltration) over installed agent tool sets. See `handoff.md`.
2. **Adversarial Swarm** (`tools/swarm/`) — deterministic, LLM-free multi-role harness that maps detection boundaries of the repo's own Sigma/YARA rules via mutation probes. This handoff's scope.

Share target: Kyle's Director + engineers. The lab is genuinely impressive (real external measurements, self-review discipline) but the endurance→cable number layer was inflated/fabricated; the fixes + errata in this session make it survivable, and the remaining open threads make it *fully* defensible.

## What happened 2026-09-10 (current state, not narrative)

### Review findings (delivered to Kyle, 5 items)
1. Endurance→cable numbers irreconcilable (state file 11.6% vs cable 98.3%; 65/35 ratio fabrication; ~88% benign "probes"; hardcoded `+= 206`; synthesizer default constants 764/220/0.712 presented as measurements).
2. DAG walks in the endurance harness never exercised evasion (all 13,711 published walks = no-evasion baseline, MTTD structurally 0s).
3. Docs/artifacts stale: architecture.md still claimed 66.7%/72.7% as *current*; YARA report titled "— None"; `target_rule: null` regression.
4. "Autonomous multi-agent AI swarm" framing overstates (no LLM anywhere — determinism is the asset).
5. External-claim calibration (name-similarity vs adjudication, hook-type semantics) — ongoing discipline, see next-release review Gate 2.

### Code fixes (all verified by the 485-test suite)
| File | Change |
|---|---|
| `tools/swarm/detectors.py` | `YaraDetector` auto-resolves the sole rule name from source (`_single_rule_name`) → boundary maps/reports carry the rule name (fixes `null`/"— None") |
| `tools/swarm/graph_engine.py` | `run_walks(..., profile_offset=0)` — offset shifts the evasion-profile rotation |
| `tools/swarm/endurance_runner.py` | DAG suite passes `profile_offset=self.graph_walks_count`; **65/35 ratio split removed** → real per-target counters (`target_probes`); `+= 206` un-hardcoded (`benign_count + attack_variants`); final state force-written (`_save_state(force=True)`); None-safe milestone logging; `_sync_boundary_histories` writes honest per-target histories (reconcile: detected+gaps==approved; `final_resilience` null when no approved) |
| `tools/swarm/critic.py` | IPv6 rejection: routable IPv6 literals blocked (loopback/unspecified/link-local + `::1` exempt); IPv4 path unchanged |
| `tools/swarm/synthesizer.py` | Removed 764/220/0.712 defaults; `total_evals <= 0` → `ValueError` (refuse, don't fabricate); `total_evals_override=0` now respected (was `or`-swallowed) |
| `tools/swarm/cli.py` | `--synthesize-trends` catches `ValueError` → prints refusal, exit 1 |

### Docs
- **`docs/cables/ERRATA-2026-09-10.md` (NEW)** — retracts STRAT-001/002/003 headline figures with per-cable evidence:
  - STRAT-001 (764/71.2%/220): frontmatter == retired default constants exactly; 764×0.712=543.968→"544 TP" is a rounding of a constant; committed histories record 3-probe runs.
  - STRAT-002 (8,033/71.4%/2,296): no persisted run exists; 71.4% == retired default; "Zero Safety Spillage" cites `203.0.113.0/24` (RFC 5737) under RFC 2606 *and* the Critic rejects that range — self-contradictory.
  - STRAT-003 (1,959,503/98.3%/33,184): contradicts own checkpoint (1,942,597 probes / 11.6%); ~88% benign events never Critic-evaluated → 1.00 approval rate unsupported; cluster totals (33,184) ≠ state breakdown (16,239).
  - Supersedes the "underlying measurements are unchanged" sentence in the Sep 5 revision notices.
- `docs/swarm/architecture.md` — roles section now states deterministic/LLM-free; "Measured Boundary Discoveries" rewritten as a 3-cycle story (66.7/72.7 historical → REC implementation table → current 7/7=100%) with errata pointer (relative link `../cables/...`).
- STRAT-001/002/003: `[!CAUTION]` correction notices added. `docs/cables/INDEX.md`: banner + errata link.
- `handoff.md`: test count 479→485 + pointer to this doc.
- Regenerated `docs/swarm/results/boundary_map_yara.json` + `campaign_report_yara.md` — now byte-identical to committed HEAD (git diff empty). Sigma map/report unchanged (were already clean).

### Also this session (Hermes config, not repo)
Default model switched from Nous Portal to **direct DeepSeek API**: `model.provider: deepseek`, `model.default: deepseek-flash`, `model.base_url` unset (plugin supplies `https://api.deepseek.com/v1`). Key `DEEPSEEK_API_KEY` already in `hermes .env`. Canonical direct-API ids: `deepseek-flash` / `deepseek-v4-pro` (`deepseek-v4.1-flash` rejected). Verified live (PONG, billing record shows `provider=deepseek`). Memory updated. This chat session itself remains pinned to nous-provider `deepseek/deepseek-v4-flash`.

## Files & how to run

- **Suite:** `.venv/Scripts/python.exe -m unittest discover -s tests` → 485 OK ~7.8s. **Use `.venv` (Python 3.14) — system python is 3.11 without yara/pySigma; import errors look like code failures.**
- **Coverage:** `.venv/Scripts/python.exe -m coverage run --source=tools -m unittest discover -s tests && .venv/Scripts/python.exe -m coverage report` → 86% total (`synthesizer.py` 79% — the untested zero-history path; add tests if you touch it).
- **Review reproductions:** `.venv/Scripts/python.exe docs/reviews/reproduce_review.py` → 9/9.
- **Swarm CLI:** `python -m tools.swarm.cli --target yara|sigma --max-cycles 2` → deterministic artifacts in `docs/swarm/results/`. For scratch: `--output-dir C:/Users/kyler/AppData/Local/Temp/<name>`.
- **Synthesis:** `python -m tools.swarm.cli --synthesize-trends` → currently **refuses (exit 1)** because histories are zeroed — expected until an endurance re-run repopulates them.
- **Endurance (isolated):** `EnduranceRunner(pace_seconds=0.0, serve_workbench=False, results_dir=Path(tempdir))` — always inject a temp results_dir; the default writes real `docs/swarm/results/`.
- **Workbench:** `swarm_workbench.html` — regression-tested against the Python engine (`test_swarm.py` EPIC-2 guard).

## Verification workflow & pitfalls (with costs)

- **The test suite writes real results files.** `SwarmOrchestratorEndToEndTests` uses the default output_dir, so the suite deterministically rewrites `boundary_map_*.json` + `campaign_report_*.md` (post-fix they match HEAD byte-for-byte; pre-fix they rewrote the `null` regression). Hash `docs/swarm/results` before/after any investigation (`find ... -exec md5sum`); don't trust "read-only".
- **65/35 is gone — verify by counter, not by shape.** After an endurance run, `runner.target_probes[target]` must reconcile with `boundary_history_<target>.json` (probes/approved/detected/gaps). A history that doesn't sum (`detected+gaps != approved`) is a bug, not noise.
- **`--synthesize-trends` refusal is correct behavior** on zeroed histories, not a regression. Don't re-add defaults to "make it work" — that was the fabrication path.
- **git-bash path mangling:** pass native paths to Python as `C:/...` (MSYS `/m/...` mangles). Line endings are mixed (CRLF warnings from git); artifacts written with `newline="\n"`.
- **Defender on this machine quarantines aggressively** — never download package payloads; defang cradle strings in prose (existing `handoff.md` disciplines, still binding).
- **`.coverage` is gitignored** (refreshed by coverage runs) — harmless.

## Open threads (priority order, next action first)

1. **Commit/share decision (GATE for Director share).** Everything since HEAD (Sep 4) uncommitted: ~130 dirty entries, `docs/reviews/` untracked, handoff docs untracked. Next agent: commit the 5 code modules + 3 test files + docs (incl. ERRATA) at minimum — no versioned rollback exists for them — then with Kyle: GitHub push vs zip. HEAD log has 40+ commits/merged PRs; the "nothing is committed" note in `handoff.md` is a standing decision but contradicts the git history — needs Kyle's call.
2. **Per-observation record layer (review R02, the big remaining one).** Append-only JSONL per evaluated variant: run id, rule hash, fixture hash, outcome, axis, timestamp; synthesizer + endurance aggregates derive from it; unclassified/error outcomes preserved; empty denominators → null. This is what makes "1.94M probes" auditable.
3. **Denominator semantics.** Define "probe" (attack variant vs benign event, per-suite); recommended: resilience = attack-variant TP / attack-variant probes, benign corpus reported separately as FP/SNR (noise_floor.json already does this). Publish in README measurement policy.
4. **Bounded endurance re-run → regenerate STRAT cables.** Histories are zeroed; state file holds the old contaminated run's checkpoint (50,000 cycles / 1.94M probes / 11.6%). After records + denominator land, run a bounded span, regenerate cables, keep ERRATA as provenance.
5. **Optional polish:** README/roadmap still use "autonomous swarm intelligence" language — consider "deterministic, LLM-free closed-loop harness" phrasing for the demo. CABLE-2026-001's ASCII flow shows 66.7%/72.7% — correct as history, leave it.
6. `handoff.md` (composition programme) has six marginal-analysis items — separate programme, untouched, still open.

## Pre-registered next moves (speculation labeled)

- **If `--synthesize-trends` crashes with ZeroDivisionError** after the re-run: the CLI now catches `ValueError`; a raw `synthesizer.synthesize()` guarantees `total_evals > 0` before formatting, so a crash means the guard regressed — check `synthesizer.py` lines ~57-74 first.
- **If boundary histories show fabricated-looking numbers** after an endurance run: check `_evaluate_probe` counter increments vs `_sync_boundary_histories` (both keyed on `target_probes`; if they drifted, the increment in `_evaluate_probe` is the likely culprit).
- **If someone "fixes" the 100% CLI result:** don't. It is correct for the current mutation vocabulary + tuned rules. Changing the craftsmen vocabulary changes the number — that's the regression signal, not a bug.
- **If the walk rotation test (`test_run_walks_profile_offset_rotates_evasion`) starts failing:** profiles index into `p_order`; a graph with <3 primary nodes produces duplicate profiles — the test uses `build_default_graph()` (5 nodes), keep it that way.

## Rollback

- Artifacts: `C:\Users\kyler\AppData\Local\Temp\tdl_results_backup_1789065361` (pre-session results), hash baselines `tdl_results_hash_before.txt`/`tdl_results_hash_after.txt`.
- Code: **no versioned rollback** — commit the touched modules first thing next session (thread #1), then re-run the suite to confirm 485 before any further edits.
